import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'




// imports
createApp(App).mount('#app')
// this file is just the entry point that mounts it to the dom and adds the css it doesnt do anything else rlly most of the js is inline or in composables
